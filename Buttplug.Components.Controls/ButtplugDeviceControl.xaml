<UserControl x:Class="Buttplug.Components.Controls.ButtplugDeviceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Buttplug.Components.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
		<ListBox Grid.Row="0" Name="DeviceListBox" Margin="10,10,10,10" />
		<Button Grid.Row="1" Name="ScanButton" Content="Start Scanning" Margin="10,0,0,10" HorizontalAlignment="Left" Width="95" Click="ScanButton_Click" />
	    <TextBlock Grid.Row="1" Margin="0,0,10,10" HorizontalAlignment="Right"><Hyperlink Click="DeviceTroubleShoot_Click" >Device not found?</Hyperlink></TextBlock>
	</Grid>
</UserControl>